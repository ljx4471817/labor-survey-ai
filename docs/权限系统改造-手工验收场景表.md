# 权限系统改造 · 手工验收场景表

> 关联：`docs/PRD-权限系统改造.md` §9、ADR 0015。
> 状态列：✅ 自动化已验证（单测/HTTP 冒烟）/ ⬜ 待人工在浏览器验证。
> 测试号：13985000001-4 分别扮演 系统管理员 / 区县业务管理员 / 调查员 / 市级业务管理员（实际以 .env `LSX_SYSTEM_ADMIN_PHONE` 为准）。

## 一、调查员（普通用户）

| # | 场景 | 预期 | 状态 |
|---|------|------|------|
| 1 | 调查员调任何 `/api/admin/*`（whitelist / usage / feedback / gaps / llm） | 403 | ✅ HTTP 冒烟 |
| 2 | 停用后原 token 立即失效（`/api/auth/check`） | 401 | ✅ HTTP 冒烟 |
| 3 | 登录后路由到 `/`（对话页） | 跳转对话页 | ✅ 浏览器 27/27 |

## 二、区县业务管理员

| # | 场景 | 预期 | 状态 |
|---|------|------|------|
| 4 | 本县新增/编辑/启用/停用调查员 | 成功 | ✅ HTTP 冒烟 |
| 5 | 跨县新增/编辑 | 403 | ✅ HTTP 冒烟 |
| 6 | 设置「区县」及以上层级 | 403 | ✅ HTTP 冒烟 |
| 7 | body 塞 `sys_role=系统管理员` | 被忽略，按层级推导为普通用户 | ✅ 单测 |
| 8 | 操作保护测试号 / 系统管理员账号 | 403 | ✅ HTTP 冒烟 + 单测 |
| 9 | 列表只显示本县；导出为 CSV | 本县数据 + CSV | ✅ HTTP 冒烟 |
| 10 | 批量停用：区内停用、区外/保护号/不存在跳过 | `disabled=1, skipped=[...]` | ✅ HTTP 冒烟 |
| 11 | 审计接口 | 403（仅系统管理员） | ✅ HTTP 冒烟 |
| 12 | `/whitelist-admin` 页面：层级下拉锁定「调查员」、无审计 tab、无 CSV 导入、导出 CSV | 角色化 UI | ✅ 浏览器 27/27 |
| 13 | 登录后路由到 `/whitelist-admin` | 跳转白名单页 | ✅ 浏览器 27/27 |
| 14 | 看板只显示本区域「使用情况 + 完成率」，隐藏 KB/反馈/LLM | 角色化 tab | ✅ 浏览器 27/27 |
| 15 | 完成率只读：选择测验看本县汇总 | 仅本县行 | ✅ 单测 test_quiz_stats_district_scope（区县 actor 只返回本县）+ 浏览器 UI ⬜ |

## 三、市级业务管理员

| # | 场景 | 预期 | 状态 |
|---|------|------|------|
| 16 | 新增「区县」管理岗 | 成功 | ✅ HTTP 冒烟 |
| 17 | 设置「市级」层级 | 403（不可设同级） | ✅ HTTP 冒烟 |
| 18 | 下发对象列表只含本市 | `cities == {本市}` | ✅ HTTP 冒烟 |
| 19 | publish targets 含外市号码 | 422，返回脱敏越权列表 | ✅ 单测 |
| 20 | 本市 publish/append/remove | 成功 | ✅ 单测 |
| 21 | `/quiz-admin` 目标选择只显示本市分组 | 前端隐藏区外分组 | ✅ 浏览器 27/27 |
| 22 | LLM 路由 / 反馈看板 / 审计 | 403 | ✅ HTTP 冒烟 |

## 四、省级业务管理员（模型预留）

| # | 场景 | 预期 | 状态 |
|---|------|------|------|
| 23 | 白名单可设 市级/区县/调查员；导出 xlsx | 后端范围（全省） | ✅ 单测（纯函数）+ ⬜ 浏览器 |
| 24 | 不下发边界：targets 限全省 | 后端 in_scope | ✅ 单测 |

> 说明：省级业务管理员本期无独立界面，登录后路由到 `/quiz-admin`（同市级）。

## 五、系统管理员

| # | 场景 | 预期 | 状态 |
|---|------|------|------|
| 25 | 全量白名单（含 sys_role 列） | 成功 | ✅ HTTP 冒烟 |
| 26 | whoami 返回 sys_role=系统管理员 | 成功 | ✅ HTTP 冒烟 |
| 27 | 审计记录查询（含 target 过滤） | 倒序 + 脱敏 | ✅ HTTP 冒烟 + 单测 |
| 28 | 停用保护测试号 | 成功 | ✅ HTTP 冒烟 |
| 29 | LLM 路由查看 | 200 | ✅ HTTP 冒烟 |
| 30 | CSV 批量导入 | 成功 + import 审计 | ✅ HTTP 冒烟 |
| 31 | 设置/修改 sys_role | 成功 + sys_role_change 审计 | ✅ 单测 |
| 32 | `/whitelist-admin`：sys_role 下拉、审计 tab、CSV 导入、导出 xlsx | 全功能 UI | ✅ 浏览器 27/27 |
| 33 | 看板全量 tab（KB/使用监测/使用侧发现/完成率） | 全功能 | ✅ 浏览器 27/27 |

## 六、回归

| # | 场景 | 预期 | 状态 |
|---|------|------|------|
| 34 | 全量单测 | `pytest tests/ -q` 全绿（198 passed） | ✅ |
| 35 | 检索评估 | `python scripts/run_eval.py --phone 13985000001` 104/104 | ✅ |
| 36 | 迁移 dry-run | `python scripts/migrate_whitelist_rbac.py --dry-run` 输出 diff + 审计表状态 | ✅ |
| 37 | 迁移 apply | 备份到 `backend/data/backups/` 后迁移 | ⬜ 上线时执行 |
| 38 | `.env` 配置 `LSX_SYSTEM_ADMIN_PHONE` | 系统管理员专属功能可用 | ⬜ 待配置 |

## 手工验收记录（浏览器）

| 日期 | 场景范围 | 结果 | 备注 |
|------|---------|------|------|
| 2026-08-13 | #3,12,13,14,21,32,33 浏览器验收（Playwright headless，临时 DB） | 27/27 通过 | 登录路由×4、白名单页角色化×10、dashboard 角色化×7、quiz-admin 范围×2 及其它 |
